[profile.default]
# Fail fast disabled to allow full test suite visibility.
fail-fast = false
# Retries disabled; adjust if flaky tests appear.
retries = 0
# Status level: show failing tests only (adjust to "all" for verbose).
status-level = "fail"

[profile.default.junit]
path = "junit.xml"
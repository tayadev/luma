Program(
  statements: [
    Return(Table([
      (Identifier("a"), Number(1.0)),
      (Identifier("b"), Boolean(true)),
      (Identifier("c"), Table([
        (Identifier("nested"), Null),
      ])),
    ])),
  ],
)